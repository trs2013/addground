<% present feed do |feed_presenter| %>
  <%= content_tag :li, class: feed_presenter.classes, data: {feed_id: feed.id } do %>
	  <%= feed_presenter.feed_link do %>
	    <%= feed_presenter.favicon(feed.host) %>
	    <%= content_tag :span, '', class: 'count' %>
	    <%= feed.title %>
	  <% end %>
	<% end %>
<% end %>